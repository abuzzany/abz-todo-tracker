<% if notice.present? %>
  <p class="mb-6 rounded-lg bg-green-50 px-4 py-2 text-sm text-green-800"><%= notice %></p>
<% end %>
<% if alert.present? %>
  <p class="mb-6 rounded-lg bg-red-50 px-4 py-2 text-sm text-red-800"><%= alert %></p>
<% end %>
