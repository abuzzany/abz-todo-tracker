<% content_for :title, "Editing to do item" %>

<h1 class="mb-6 text-3xl font-bold text-gray-900">Editing to do item</h1>

<%= render "form", to_do_item: @to_do_item %>

<div class="mt-4 flex items-center gap-4">
  <%= link_to "Show this to do item", @to_do_item, class: "text-sm font-medium text-blue-600 hover:underline" %>
  <%= link_to "Back to to do items", to_do_items_path, class: "text-sm font-medium text-blue-600 hover:underline" %>
</div>
